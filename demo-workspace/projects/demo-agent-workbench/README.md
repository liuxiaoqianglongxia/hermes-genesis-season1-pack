# Demo Agent Workbench

这是一个虚构项目，用来模拟“AI Agent 工作台”的最小项目事实源。

核心文件：

- `STATE.md`：当前状态和边界。
- `docs/progress.md`：最近进展。

建议让 AI 先读这两个文件，再读 `../../registry.yaml` 和 `../../teams/team-registry.example.yaml`。
